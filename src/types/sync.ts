/**
 * @description Options de rafraîchissement déclenché par la sync Supabase.
 */
export type SyncReloadOptions = {
  /** Ne pas afficher l'état de chargement (sync arrière-plan). */
  silent?: boolean;
};
